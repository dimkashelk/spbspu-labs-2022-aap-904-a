#include "issum.hpp"
bool isSum(int preprevalue, int prevalue, int value)
{
  bool cond = true;
  if (preprevalue + prevalue == value)
  {
   return cond;
  }
  else
  {
   cond = false;
   return cond;
  }
  return cond;
}
