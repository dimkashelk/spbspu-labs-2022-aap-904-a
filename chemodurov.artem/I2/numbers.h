#ifndef NUMBERS_H
#define NUMBERS_H
#include <cstddef>

namespace chemodurov
{
  unsigned long int countMaxDecreasing(const int * arr, size_t size);
  unsigned long int countDivisible(const int * arr, size_t size, int divider);
}
#endif
