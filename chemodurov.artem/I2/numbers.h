#ifndef NUMBERS_H
#define NUMBERS_H
#include <cstddef>

namespace chemodurov
{
  size_t countMaxDecreasing(const int * arr, size_t size);
  size_t countDivisible(const int * arr, size_t size, int divider);
}
#endif
