#ifndef SUMSTRINGS_HPP
#define SUMSTRINGS_HPP

#include <cstddef>

void sum_strings(char *arr_sum, unsigned int arr_sum_len, const char *cstring_one, unsigned int size_one,
                 const char *cstring_two,
                 unsigned int size_two);

#endif
