#ifndef ALGORITHIMSARRAYS_H
#define ALGORITHIMSARRAYS_H
#include <cstddef>
size_t checkRepeatEl(const int* array, size_t size, size_t index_n, size_t index_m);
size_t counterDivision(const int* array, size_t size,unsigned int n);


#endif
