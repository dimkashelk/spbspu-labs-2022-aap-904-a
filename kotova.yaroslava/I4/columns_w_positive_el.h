#ifndef COLUMNS_W_POSITIVE_EL_H
#define COLUMNS_W_POSITIVE_EL_H
#include <cstddef>
size_t countPositiveElOfCol(const int* matrix, size_t column, size_t line);
bool isPositive(const int* matrix, size_t line,size_t columns, size_t& number_column);
#endif
