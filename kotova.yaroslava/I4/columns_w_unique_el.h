#ifndef COLUMNS_W_UNIQUE_EL_H
#define COLUMNS_W_UNIQUE_EL_H
#include <cstddef>
size_t countUniqueElOfCol(const int* matrix, size_t column, size_t line);
bool isUnique(const int* matrix, size_t column, size_t line);
#endif
