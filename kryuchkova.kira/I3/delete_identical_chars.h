#ifndef DELETE_IDENTICAL_CHARS_H
#define DELETE_IDENTICAL_CHARS_H
#include <cstddef>

char *delete_identical_chars(size_t size_first, const char *cstring_first, size_t size_second, const char *cstring_second);

#endif
