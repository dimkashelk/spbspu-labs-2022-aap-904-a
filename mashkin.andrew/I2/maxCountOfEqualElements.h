#ifndef MAXCOUNTOFEQUALELEMENTS_H
#define MAXCOUNTOFEQUALELEMENTS_H
#include <cstddef>

void countMaxQuantityOfEqualElements(const int* arr, const size_t size, size_t& count, size_t& maxCount);
#endif
