#ifndef METHODS_H
#define METHODS_H
#include <cstddef>

void count_of_maxes(const int* arr, size_t size, int& maximum, size_t& count_maximum);
void shift_part_to_end(int* arr, size_t size, unsigned int m, unsigned int n);

#endif
