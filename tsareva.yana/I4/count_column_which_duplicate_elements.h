#ifndef COUNT_COLUMN_WHICH_DUPLICATE_ELEMENTS_H
#define COUNT_COLUMN_WHICH_DUPLICATE_ELEMENTS_H
#include <cstddef>
size_t countColumnWhichDuplicateElements(const int * array, size_t row, size_t column);
#endif
