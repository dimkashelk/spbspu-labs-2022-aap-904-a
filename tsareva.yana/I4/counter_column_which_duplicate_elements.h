#ifndef COUNTER_COLUMN_WHICH_DUPLICATE_ELEMENTS_H
#define COUNTER_COLUMN_WHICH_DUPLICATE_ELEMENTS_H
#include <cstddef>
size_t counterColumnWhichDuplicateElements(const int * array, const size_t row, size_t column);
#endif
