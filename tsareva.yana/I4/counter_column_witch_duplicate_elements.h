#ifndef COUNTER_COLUMN_WITCH_DUPLICATE_ELEMENTS_H
#define COUNTER_COLUMN_WITCH_DUPLICATE_ELEMENTS_H
#include <cstddef>
size_t counterColumnWitchDuplicateElements(const int * array, const size_t row, size_t column);
#endif
