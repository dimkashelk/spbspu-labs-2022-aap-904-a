#ifndef METHODS_H
#define METHODS_H
#include "cString.h"
namespace turkin
{
  turkin::Array deleteNumbers(const turkin::Array & string);
  bool isRepeat(const turkin::Array & string);
  bool isNumber(char symbol);
}
#endif

